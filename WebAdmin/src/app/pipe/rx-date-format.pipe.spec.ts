import { RxDateFormatPipe } from './rx-date-format.pipe';

describe('RxDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RxDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
